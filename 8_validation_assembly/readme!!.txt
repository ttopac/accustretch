Place the following files in the "designFiles" folder before running the code:
1) .csv file generated after solving optimization via Docplex (or something else). This will give us information about node locations. Filename: serpentine_NTN.csv
2) .stp files generated by Optimus with increasing experiment number. Filenames: 0.stp, 1.stp, .... 
	Format: Increasing first from left to right starting top row. After all rows are done increasing from top to bottom starting from left column.

Things to do:
1) Edit number of sensor rows and column. Note we always take increasing x to right (column count) increasing z to bottom (row count). Second dimension here is different than our surrogate convention where y was increasing up.
2) Always open the Serpentine_Assembly_generic.cae file